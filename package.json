{
  "name": "bramagrams",
  "version": "1.0.0",
  "repository": "https://github.com/nkosarek/bramagrams",
  "author": "nkosarek",
  "license": "MIT",
  "scripts": {
    "install": "yarn install --cwd client && yarn install --cwd server",
    "postinstall": "ls -al && ls -al server && ls -al server/node_modules/.bin && ls -al client",
    "build": "cd client && yarn build && cd ../server && yarn build && cd ..",
    "start": "yarn server --cwd server"
  },
  "engines": {
    "node": "14.x",
    "yarn": "1.x"
  }
}
