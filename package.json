{
  "name": "bramagrams",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "postinstall": "tsc -b -v shared && cd client && yarn install && cd ../server && yarn install",
    "build": "tsc -b -v server && cd client && yarn build",
    "start": "NODE_ENV=production node server",
    "clean": "rm -rf client/build && rm -rf server/dist && rm -rf shared/dist"
  },
  "repository": "https://github.com/nkosarek/bramagrams",
  "author": "nkosarek",
  "license": "MIT",
  "dependencies": {
    "typescript": "^3.9.7"
  }
}